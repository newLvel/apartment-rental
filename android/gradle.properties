org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Enable Gradle to automatically download a matching JDK for the Java toolchain
# (Gradle will attempt to provision JDK 21 for the configured toolchain).
org.gradle.java.installations.auto-download=true

# If you prefer to use a locally installed JDK 21, set the path here (uncomment and update):
# org.gradle.java.home=C:/Program Files/Java/jdk-21
